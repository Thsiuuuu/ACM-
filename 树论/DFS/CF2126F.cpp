#include <iostream>
#include <map>
#include <algorithm>
#include <vector>
using namespace std;
/*


    每个点按照颜色建立哈希表

*/

#define int long long 
const int N=1e5+10;
int t;
int n,q;
vector<int> e[N];
vector<int> wgt[N];
void sol(){
    cin>>n>>q;
    vector<map<int>> col(n+1);

}
signed main(){
    ios::sync_with_stdio(0);
    cin.tie(0),cout.tie(0);

    cin>>t;
    while(t--){
        sol();
    }
    return 0;
}