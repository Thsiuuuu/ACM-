#include <iostream>
#include <string>

using namespace std;

/*

    建树，然后dfs遍历过程中设置最早祖先

*/

const int N=5e4+10;
int st[N];
struct node{
    string s;
    int idx;
}ppl[N];

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0),cout.tie(0);

    string a;
    while(cin>>a){
        if(a=="$") break;
        if(a[0]=="#"){

        }
    }
}




