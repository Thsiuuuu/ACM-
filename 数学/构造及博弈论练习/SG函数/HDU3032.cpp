#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
using namespace std;

#define int long long 
const int N=1e6+10;

int t;
int stn[N];
int  sg(){

}

void sol(){
    int n;
    for(int i=0;i<n;i++) cin>>stn[i];
    
}

signed main(){
    ios::sync_with_stdio(0);
    cin.tie(0),cout.tie(0);

    cin>>t;
    while(t--){
        sol();
    }
    return 0;
}